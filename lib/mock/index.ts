/**
 * Mock data barrel. The data provider imports from here so that swapping in a
 * different city or a live source is a one-line change.
 */
export { bangkokPlaces as mockPlaces, bangkokEvents as mockEvents } from "./bangkok";
